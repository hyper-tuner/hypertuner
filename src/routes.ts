// eslint-disable-next-line import/prefer-default-export
export enum Routes {
  ROOT = '/',
  TAB = '/:tab',
  TUNE = '/tune',
  DIALOG = '/tune/:category/:dialog',
  LOG = '/log',
  DIAGNOSE = '/diagnose',
}
